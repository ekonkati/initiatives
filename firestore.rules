
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    
    // --- Helper Functions ---

    function isUserAuthenticated() {
      return request.auth != null;
    }

    function isAdmin() {
      // Check if the user document exists and the role is 'Admin'.
      return isUserAuthenticated() && get(/databases/$(database)/documents/users/$(request.auth.uid)).data.role == 'Admin';
    }

    function isOwner(userId) {
      return isUserAuthenticated() && request.auth.uid == userId;
    }

    function isInitiativeMember(initiativeId) {
      let initiativeDoc = get(/databases/$(database)/documents/initiatives/$(initiativeId));
      let isLead = request.auth.uid in initiativeDoc.data.leadIds;
      let isTeamMember = request.auth.uid in initiativeDoc.data.teamMemberIds;
      
      return isUserAuthenticated() && (isAdmin() || isLead || isTeamMember);
    }
    
    // This rule is separate to handle the collection-level query for initiatives.
    // Admins can list all initiatives.
    // Other users can list initiatives where their UID is in the leadIds or teamMemberIds.
    // The client query MUST match this condition.
    match /initiatives/{initiativeId} {
        allow read, write: if isInitiativeMember(initiativeId);
    }
    
    // The initiatives collection can be listed if the user is an admin OR
    // if the query constrains the search to initiatives where the user is a member.
    match /initiatives/{initiativeId} {
       allow list: if isAdmin() || (request.query.where.in_ == [['leadIds', 'array-contains', request.auth.uid], ['teamMemberIds', 'array-contains', request.auth.uid]]);
    }

    // --- Rules ---

    // USERS
    match /users/{userId} {
      // Any authenticated user can read any profile (for avatars/names).
      allow read: if isUserAuthenticated();
      // Users can update their own profile, or an admin can.
      allow write: if isOwner(userId) || isAdmin();
    }

    // DEPARTMENTS & DESIGNATIONS
    match /departments/{docId} {
      // Any authenticated user can read master data.
      allow read: if isUserAuthenticated();
      // Only admins can write master data.
      allow write: if isAdmin();
    }
    match /designations/{docId} {
        // Any authenticated user can read master data.
      allow read: if isUserAuthenticated();
       // Only admins can write master data.
      allow write: if isAdmin();
    }

    // INITIATIVES & SUBCOLLECTIONS
    match /initiatives/{initiativeId} {
      
      // TASKS, ATTACHMENTS, and other subcollections
      // Inherit the parent initiative's membership check for all operations.
      match /{allChildren=**} {
        allow read, write: if isInitiativeMember(initiativeId);
      }
    }
  }
}
